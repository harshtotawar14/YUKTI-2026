(() => {
  'use strict';
  let busy=false;
  let timer=null;
  let lastWorkerSignature='';
  let lastCustomerSignature='';

  async function req(path,opt={}){const r=await fetch(path,{...opt,credentials:'include',headers:{'Content-Type':'application/json',...(opt.headers||{})},cache:'no-store'});const d=await r.json().catch(()=>({}));if(!r.ok){const e=new Error(d.message||d.error||`Request failed (${r.status})`);e.status=r.status;throw e;}return d;}
  const post=(p,b={})=>req(p,{method:'POST',body:JSON.stringify(b)});
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const money=n=>'₹'+Number(n||0).toLocaleString('en-IN',{minimumFractionDigits:0,maximumFractionDigits:2});
  const human=s=>String(s||'').toLowerCase().replaceAll('_',' ').replace(/\b\w/g,c=>c.toUpperCase());
  function open(){const s=document.getElementById('connectedShell');return !!(s&&!s.classList.contains('hidden'));}
  function role(){return document.getElementById('connectedContent')?.dataset.connectedRole||'';}
  function host(id,anchorId){let h=document.getElementById(id);if(h)return h;const a=document.getElementById(anchorId);if(!a)return null;h=document.createElement('div');h.id=id;h.style.marginTop='14px';a.insertAdjacentElement('afterend',h);return h;}
  function friendly(e){const s=Number(e?.status||0);if(s===401)return 'Your demo session expired. Please log in again.';if(s===409)return 'This action was already completed on another device.';if(s>=500)return 'Service temporarily unavailable. Please retry.';return e?.message||'This action could not be completed. Please retry.';}
  function setMarkup(h,signature,html,type){const current=type==='worker'?lastWorkerSignature:lastCustomerSignature;if(current===signature)return false;if(type==='worker')lastWorkerSignature=signature;else lastCustomerSignature=signature;h.innerHTML=html;return true;}
  async function refresh(){if(!open()||document.hidden||busy)return;const r=role();if(r==='WORKER')await worker();if(r==='CUSTOMER')await customer();}

  async function worker(){
    const h=host('connectedWorkerCommerce','connectedLifecycleHost');if(!h)return;
    try{const offers=await req('/api/connected/worker/offers');const o=offers.find(x=>x.offerStatus==='ACCEPTED'&&['IN_PROGRESS','AWAITING_CUSTOMER_CONFIRMATION','COMPLETED','PAID'].includes(x.status));if(!o||o.status!=='IN_PROGRESS'){if(lastWorkerSignature!=='EMPTY'){h.innerHTML='';lastWorkerSignature='EMPTY';}return;}const signature=JSON.stringify([o.bookingId,o.status]);const html=`<div class="connected-card"><span class="connected-step-label">OPTIONAL EXTRA WORK</span><h3>Request Additional Work Approval</h3><p>Extra work changes the final bill only after the customer approves it.</p><form id="connectedExtraForm" class="connected-form"><div class="field"><label>Work Item</label><input id="extraWorkItem" value="Replace damaged switch" required></div><div class="field"><label>Reason</label><input id="extraReason" value="Damaged component found after inspection"></div><div class="field"><label>Amount</label><input id="extraAmount" type="number" min="1" max="10000" value="150" required></div><button class="btn secondary" id="extraSubmit" type="submit">Request Customer Approval</button><div id="connectedCommerceMessage"></div></form></div>`;if(setMarkup(h,signature,html,'worker'))h.querySelector('#connectedExtraForm').onsubmit=async e=>{e.preventDefault();if(busy)return;busy=true;const b=h.querySelector('#extraSubmit');const old=b.textContent;b.disabled=true;b.textContent='Sending request…';try{await post(`/api/connected/worker/jobs/${o.bookingId}/extra-charge`,{workItem:h.querySelector('#extraWorkItem').value.trim(),reason:h.querySelector('#extraReason').value.trim(),amount:Number(h.querySelector('#extraAmount').value)});msg('Additional work request sent to the customer for approval.');}catch(err){msg(friendly(err),'error');}finally{busy=false;b.disabled=false;b.textContent=old;}};}catch(e){if(lastWorkerSignature!=='ERR'){h.innerHTML=`<div class="connected-error">${esc(friendly(e))}</div>`;lastWorkerSignature='ERR';}}}

  async function customer(){
    const h=host('connectedCustomerCommerce','connectedCustomerVerificationHost');if(!h)return;const id=Number(localStorage.getItem('sanpaid_connected_booking_id')||0);if(!id){if(lastCustomerSignature!=='EMPTY'){h.innerHTML='';lastCustomerSignature='EMPTY';}return;}
    try{const [checkout,charges]=await Promise.all([req(`/api/connected/customer/bookings/${id}/checkout`),req(`/api/connected/customer/bookings/${id}/charges`)]);const signature=JSON.stringify([checkout.status,checkout.total,checkout.approvedAdditional,checkout.finalAmount,checkout.payment?.transactionReference,checkout.invoice?.invoiceNumber,(charges||[]).map(c=>[c.id,c.status,c.amount,c.workItem])]);const chargeHtml=charges.length?charges.map(c=>`<div class="connected-state-line"><div class="connected-heading-row"><b>${esc(c.workItem)}</b><b>${money(c.amount)}</b></div><p>${esc(c.reason||'Additional work')}</p><span class="badge ${c.status==='APPROVED'?'b-green':c.status==='REJECTED'?'b-gray':'b-orange'}">${esc(human(c.status))}</span>${c.status==='PENDING'?`<div class="connected-actions"><button class="btn danger small" data-charge="${c.id}" data-decision="REJECT">Reject</button><button class="btn primary small" data-charge="${c.id}" data-decision="APPROVE">Approve</button></div>`:''}</div>`).join(''):'<div class="connected-empty">No additional work charges.</div>';const payable=['COMPLETED','PAYMENT_PENDING','PAID'].includes(checkout.status);const paid=!!checkout.payment;const html=`<div class="connected-card"><span class="connected-step-label">CHECKOUT</span><h3>Charges, Sandbox Payment & Invoice</h3><p>Only customer-approved extra work is included in the final amount.</p><div class="connected-list">${chargeHtml}</div><div class="connected-divider"></div><div class="connected-meta"><div>Service Amount<b>${money(checkout.total)}</b></div><div>Approved Extra Work<b>${money(checkout.approvedAdditional)}</b></div><div>Final Amount<b>${money(checkout.finalAmount)}</b></div><div>Status<b>${esc(human(checkout.status))}</b></div></div>${payable&&!paid?`<div class="connected-demo-note"><b>SANDBOX PAYMENT</b> · No real money will move.</div><div class="connected-actions"><select id="connectedPayMethod" class="voice-lang-select"><option value="DEMO">Demo</option><option value="UPI">UPI · Sandbox</option><option value="CARD">Card · Sandbox</option><option value="CASH">Cash Record</option></select><button class="btn primary" id="connectedPay">Complete Sandbox Payment</button></div>`:''}${paid?`<div class="connected-success connected-payment-success"><b>Payment Successful — Sandbox ✓</b><br>Transaction ID: ${esc(checkout.payment.transactionReference)}<br>Method: ${esc(checkout.payment.paymentMethod)}<br>Amount: ${money(checkout.payment.amount)}${checkout.invoice?`<br>Invoice: <b>${esc(checkout.invoice.invoiceNumber)}</b>`:''}</div>${checkout.invoice?`<div class="connected-actions"><button class="btn secondary" type="button" id="connectedPrintInvoice">Print Invoice</button></div>`:''}<div class="connected-divider"></div><form id="connectedRatingForm" class="connected-form"><div class="field"><label>Rating</label><select id="connectedStars"><option value="5">5 — Excellent</option><option value="4">4 — Good</option><option value="3">3 — Average</option><option value="2">2 — Needs Improvement</option><option value="1">1 — Poor</option></select></div><div class="field"><label>Feedback</label><textarea id="connectedFeedback">Professional service and clear communication.</textarea></div><button class="btn primary" id="connectedRatingSubmit" type="submit">Submit Rating</button></form>`:''}<div id="connectedCommerceMessage"></div></div>`;if(setMarkup(h,signature,html,'customer')){h.querySelectorAll('[data-charge]').forEach(b=>b.onclick=()=>decide(Number(b.dataset.charge),b.dataset.decision,b));h.querySelector('#connectedPay')?.addEventListener('click',e=>pay(id,h.querySelector('#connectedPayMethod').value,e.currentTarget));h.querySelector('#connectedRatingForm')?.addEventListener('submit',e=>rate(e,id));h.querySelector('#connectedPrintInvoice')?.addEventListener('click',()=>window.print());}}
    catch(e){if(lastCustomerSignature!=='ERR'){h.innerHTML=`<div class="connected-error">${esc(friendly(e))}</div>`;lastCustomerSignature='ERR';}}
  }

  function msg(text,type='success'){const e=document.getElementById('connectedCommerceMessage');if(e)e.innerHTML=`<div class="${type==='error'?'connected-error':'connected-success'}" style="margin-top:10px">${esc(text)}</div>`;}
  async function decide(id,decision,button){if(busy)return;busy=true;const old=button.textContent;button.disabled=true;button.textContent=decision==='APPROVE'?'Approving…':'Rejecting…';try{await post(`/api/connected/customer/charges/${id}/decision`,{decision});msg(`Additional charge ${decision==='APPROVE'?'approved':'rejected'}.`);lastCustomerSignature='';}catch(e){msg(friendly(e),'error');button.disabled=false;button.textContent=old;}finally{busy=false;setTimeout(refresh,180);}}
  async function pay(id,method,button){if(busy)return;busy=true;const old=button.textContent;button.disabled=true;button.textContent='Processing Sandbox Payment…';try{const r=await post(`/api/connected/customer/bookings/${id}/pay`,{method});msg(`Sandbox payment recorded: ${r.payment?.transactionReference||'success'}.`);lastCustomerSignature='';}catch(e){msg(friendly(e),'error');button.disabled=false;button.textContent=old;}finally{busy=false;setTimeout(refresh,180);}}
  async function rate(event,id){event.preventDefault();if(busy)return;busy=true;const b=document.getElementById('connectedRatingSubmit');const old=b?.textContent;if(b){b.disabled=true;b.textContent='Saving rating…';}try{await post(`/api/connected/customer/bookings/${id}/rating`,{stars:Number(document.getElementById('connectedStars').value),feedback:document.getElementById('connectedFeedback').value.trim()});msg('Rating saved to the worker service profile.');}catch(e){msg(friendly(e),'error');if(b){b.disabled=false;b.textContent=old;}}finally{busy=false;}}

  function schedule(delay=5600){clearTimeout(timer);timer=setTimeout(async()=>{await refresh();schedule(document.hidden?9000:5600);},delay);}
  function start(){setTimeout(refresh,600);schedule();document.addEventListener('visibilitychange',()=>{if(!document.hidden)refresh();});document.addEventListener('click',e=>{if(e.target.closest('[data-open-connected],#connectedDemoBtn,[data-connected-persona]'))setTimeout(refresh,600);},true);}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
})();